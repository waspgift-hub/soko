# Railway deployment — set these in Railway dashboard
# -------------------------------------------------
# Required:
MONGIKE_API_KEY=mk_684dc4960dd0a783657d2bdb4b7812468e4dd2578c7b01b7
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"sokonimoko-8c171-a8d14",...}
ADMIN_SECRET=sk_soko_admin_9f8a2b3c4d5e6f7g8h9i0j
WEBHOOK_SECRET=wh_soko_1a2b3c4d5e6f7g8h9i0j1k2l
WEBHOOK_URL=https://soko-langu-production.up.railway.app/api/webhook

# Optional (Railway sets PORT automatically):
PORT=3000

# SMTP for OTP emails (uses Gmail App Password):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=waspgift@gmail.com
SMTP_PASS=fgnd ylot wwmc grou
SMTP_FROM=Soko Langu <waspgift@gmail.com>
SMTP_SECURE=false

# Escrow config:
ESCROW_AUTO_RELEASE_DAYS=14

# Fraud prevention:
MAX_DAILY_SALE_AMOUNT=5000000

# ─── How to deploy on Railway ───
# 1. Push this folder to a GitHub repo
# 2. In Railway, click "New Project" → "Deploy from GitHub repo"
# 3. Add all env vars above in Railway dashboard (settings → variables)
# 4. For FIREBASE_SERVICE_ACCOUNT_JSON, paste the entire JSON (single line, no newlines)
# 5. Railway will auto-detect Node.js and run `npm start` (node index.js)
# 6. The /health endpoint confirms the server is running
